# 可选。匿名请求被限制时，从已登录浏览器会话中获取 Cookie 并仅保存在本地环境变量。
EASTMONEY_COOKIE=

# OpenAI Responses API。密钥只设置在后端环境变量中，不能暴露给浏览器。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT=120

# 多个开发地址用逗号分隔。
CORS_ORIGIN=http://127.0.0.1:3000,http://localhost:3000
